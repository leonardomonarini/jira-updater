name: Jira Updater
description: Update fields of a specific JIRA Issue
branding:
  icon: 'chevron-right'
  color: 'blue'
inputs:
  issue:
    description: Key of the issue to be transitioned
    required: true
  reporter:
    description: Version to update the JIRA fixVersions field
    required: true
runs:
  using: 'node12'
  main: './dist/index.js'
